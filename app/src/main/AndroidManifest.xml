<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edu_s.infocarmadrid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MiscochesActivity"
            android:label="@string/title_activity_miscoches"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".comprobarcocheActivity"
            android:label="@string/title_activity_comprobarcoche"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".recordatorioActivity"
            android:label="@string/title_activity_recordatorio"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>